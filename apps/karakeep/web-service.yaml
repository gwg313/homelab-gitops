apiVersion: v1
kind: Service
metadata:
  name: karakeep-web
  namespace: karakeep
spec:
  selector:
    app: karakeep-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
