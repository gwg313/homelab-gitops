apiVersion: v1
kind: Service
metadata:
  name: karakeep-chrome
  namespace: karakeep
spec:
  selector:
    app: karakeep-chrome
  ports:
    - name: cdp
      port: 9222
      targetPort: 9222
  type: ClusterIP
