apiVersion: v1
kind: Service
metadata:
  name: karakeep-meilisearch
  namespace: karakeep
spec:
  selector:
    app: karakeep-meilisearch
  ports:
    - name: http
      port: 7700
      targetPort: 7700
  type: ClusterIP
