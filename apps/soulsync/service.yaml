apiVersion: v1
kind: Service
metadata:
  name: slskd
  namespace: slskd
spec:
  selector:
    app: slskd
  ports:
    - name: http
      port: 80
      targetPort: 5030
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: soulsync
  namespace: slskd
spec:
  selector:
    app: slskd
  ports:
    - name: http
      port: 80
      targetPort: 8008
  type: ClusterIP
