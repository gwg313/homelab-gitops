---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: quay-cert
  namespace: istio-system
spec:
  secretName: quay-cert
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
    - quay.gwg313.xyz
